# Canary staleness check.
# Stage 7: this workflow will verify the CANARY.md "Last updated"
# date, confirm the presence of valid PGP signatures from at least
# two maintainers, and open an issue if either check fails.
name: Canary

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Canary staleness check not yet configured."
