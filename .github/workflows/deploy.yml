# Deploy pipeline.
# Stage 3: this workflow will build the Astro site and deploy to
# Cloudflare Pages via the Cloudflare Pages GitHub Action on merges
# to main.
name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Deploy pipeline not yet configured."
