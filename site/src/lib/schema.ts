/**
 * Stage 2 stub.
 *
 * The canonical judge schema is now JSON Schema at
 * ../../../data/schema/judge.schema.json, and the field taxonomy is at
 * ../../../data/schema/fields.yaml. This TypeScript file will be
 * rewritten in Stage 3 to consume those canonical sources and render
 * real judge cards.
 *
 * Nothing schema-related is exported from here in Stage 2.
 */

export {};
