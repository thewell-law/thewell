[project]
name = "thewell-scripts"
version = "0.0.0"
description = "Operational and build tooling for The Well."
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "jsonschema>=4.21",
    "click>=8.1",
]

[tool.uv]
package = false
